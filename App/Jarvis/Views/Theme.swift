import SwiftUI

// The Android app's palette, so the two companions read as siblings.
enum Palette {
    static let accent = Color(red: 0.05, green: 0.45, blue: 0.42)
    static let userBubble = Color(red: 0.78, green: 0.87, blue: 0.86)
    static let assistantBubble = Color(uiColor: .secondarySystemBackground)
    static let errorText = Color(red: 0.7, green: 0.15, blue: 0.1)
}
